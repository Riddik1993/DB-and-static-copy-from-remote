{
  connection {
    host = ${?REMOTE_HOST}
    user = ${?REMOTE_USER}
    port = 22
  }

  load-params {
   remote-dbcopy-folder = /
   local-dbcopy-folder = /
   remote-media-path = /
   local-media-path = /
   old-db-copies-exp-period = 14
  }
}

